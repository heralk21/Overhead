//
//  FlightTrackerWidgetControl.swift
//  FlightTrackerWidget
//
//  Created by Heral Kumar on 2026-05-14.
//
//  Removed the sample ControlWidget (Xcode template boilerplate). It relied on
//  AppIntents, which generates a `Metadata.appintents` bundle that prevents the
//  widget extension from loading once the app is re-signed with a different
//  bundle ID (sideloading). The app does not use a Control widget.
